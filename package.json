{
  "name": "lightniing",
  "private": true,
  "version": "0.1.0",
  "description": "Markdown-driven project site for lightNIIng NeuroImaging Infrastructure.",
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "preview": "bun run scripts/build.ts && bun run scripts/preview.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "marked": "^14.1.4",
    "typescript": "^6.0.3"
  }
}
